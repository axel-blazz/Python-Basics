try :
    x = int(input('Input an integer : '))
    print(x)
except :
    print('Not an integer')

# Basically like an error statement so that to check exception cases. will not throw error even if not integer but runs except.